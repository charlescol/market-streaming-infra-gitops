apiVersion: v1
kind: Service
metadata:
  name: schema-registry-lb
  namespace: backend
spec:
  type: NodePort
  ports:
    - protocol: TCP
      port: 8081     
      targetPort: 8081 
      nodePort: 30081  
apiVersion: v1
kind: Service
metadata:
  name: druid-postgres-hl
  namespace: backend
  labels:
    app: druid-postgres
spec:
  clusterIP: None # headless
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
  selector:
    app: druid-postgres
---
apiVersion: v1
kind: Service
metadata:
  name: druid-postgres
  namespace: backend
  labels:
    app: druid-postgres
spec:
  type: ClusterIP
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
  selector:
    app: druid-postgres
---
apiVersion: v1
kind: Service
metadata:
  name: druid-router
  namespace: backend
spec:
  selector:
    app: druid
    component: router
  ports:
    - name: http
      port: 8088
      targetPort: 8088
  type: ClusterIP

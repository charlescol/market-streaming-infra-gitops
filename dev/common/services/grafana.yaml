apiVersion: v1
kind: Service
metadata:
  name: grafana-lb
  namespace: backend
  annotations:
    networking.gke.io/load-balancer-ipv4: 35.194.99.139
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
  selector:
    app.kubernetes.io/name: grafana

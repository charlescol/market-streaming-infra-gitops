apiVersion: v1
kind: Service
metadata:
  name: grafana-lb
  namespace: backend
spec:
  type: LoadBalancer
  loadBalancerIP: 35.194.99.139
  ports:
    - name: https
      port: 443
      targetPort: 3000
      protocol: TCP
  selector:
    app.kubernetes.io/name: grafana

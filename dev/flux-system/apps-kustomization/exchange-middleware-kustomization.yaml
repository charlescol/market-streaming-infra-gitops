apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: app-exchange-middleware
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./qa/apps/exchange-middleware
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
    - name: app-tokio-binance-mock

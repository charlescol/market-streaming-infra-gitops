apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: prometheus-tokio-binance-scraper
  namespace: backend
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: tokio-binance-scraper-metrics
  namespaceSelector:
    matchNames:
      - backend
  endpoints:
    - port: "tcp-prometheus"
      interval: 10s
      scrapeTimeout: 5s
      path: /
      scheme: http
  jobLabel: "tokio-binance-scraper-metrics"
  targetLabels:
    - tokio-binance-scraper-metrics

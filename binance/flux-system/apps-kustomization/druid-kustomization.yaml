apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: app-druid
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./binance/apps/druid
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
    - name: app-kafka
    - name: app-schema-registry

apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: app-middleware
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./binance-kucoin/apps/middleware
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
    - name: app-schema-registry
    - name: app-kafka

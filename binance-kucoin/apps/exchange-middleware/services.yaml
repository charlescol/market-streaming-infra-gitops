apiVersion: v1
kind: Service
metadata:
  name: tokio-kucoin-middleware-metrics
  namespace: backend
  labels:
    app: tokio-kucoin-middleware-metrics
spec:
  selector:
    app: tokio-kucoin-middleware
  ports:
    - protocol: TCP
      port: 9000
      targetPort: 9000
      name: tcp-prometheus
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  name: tokio-kucoin-middleware-http
  namespace: backend
  labels:
    app: tokio-kucoin-middleware-http
spec:
  selector:
    app: tokio-kucoin-middleware
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      name: tcp-http
  type: ClusterIP

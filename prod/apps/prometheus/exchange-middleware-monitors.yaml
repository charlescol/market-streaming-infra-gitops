apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: prometheus-tokio-exchange-middleware
  namespace: backend
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: tokio-exchange-middleware-metrics
  namespaceSelector:
    matchNames:
      - backend
  endpoints:
    - port: "tcp-prometheus"
      interval: 10s
      scrapeTimeout: 5s
      path: /metrics
      scheme: http
  jobLabel: "tokio-exchange-middleware-metrics"
  targetLabels:
    - tokio-exchange-middleware-metrics
